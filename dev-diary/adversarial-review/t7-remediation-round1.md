# T7 round 1 — remediation

| | |
|---|---|
|**Target**|The three round-1 findings of `t7-round1.md` (M1, M2, L1) — all PIN-ONLY. No production behaviour change was required or made: every source file is byte-identical to the round-1 manifest (`verify.go 8dfa7bea…`, `persist.go efc194ac…`, `illustrate.go 16e72216…`, `types.go 7def1112…`, all thirteen T6 files unchanged). Only the two T7-owned test files moved: `verify_test.go` `b3fd3f47…` → `fa5d9bca…`, `persist_test.go` `c76f3a20…` → `467629e6…`. `dev-diary/PLAN.md` was not touched (external in-flight T10 edit).|
|**Date**|2026-09-05. Remediation agent fresh to the round (no memory of the implementation's reasoning).|
|**Status**|COMPLETE — every row landed; each new pin red under its re-applied mutant (md5-verified restores below); gates green. Residue note at the end. Zero residue is claimed only after round 2's verdict.|

## Dispositions

| # | Finding | Disposition | Status |
|---|---|---|---|
| 1 | **M1** — the closing loop's never-fire error branches are unpinned: the regeneration render and decode returns (`verify.go:229-230`, `233-234`, both `regenerate: %w`) and `judgePage`'s no-choices and textless-reply branches (`273-274`, `277-278`, both `ErrBadVerdict`) measured zero coverage, and the no-second-retry property on the regeneration path had no pin (the retry cap counts false verdicts, not errors — a `continue` is an unbounded spin) | Five pins, scripted fakes, asserting exact render and judge call counts + zero Book + `errors.Is` on the sentinel: **`TestIllustrate_JudgeErrorMidLoopSurfacesUnretried`** (judge answers the first render false and dies with a wrapped `gmi.ErrTransient` on the regeneration's verdict — sentinel surfaces after exactly 2 renders and 2 judge calls, zero Book), **`TestIllustrate_RegenerationRenderFailureSurfacesUnretried`** (the regeneration's `EditImage` fails — wrapped sentinel surfaces, 2 renders, 1 judge call, no third render; covers 229-230), **`TestIllustrate_RegenerationDecodeFailureSurfacesUnretried`** (the regeneration's response decodes to nothing usable — `ErrUnsupportedImage` surfaces, 2 renders, no third; covers 233-234), **`TestIllustrate_NoChoicesReplyIsABadVerdict`** (empty `Choices` → `ErrBadVerdict`, 1 render, no regeneration; covers 273-274), **`TestIllustrate_TextlessAssistantReplyIsABadVerdict`** (a content array with no text → `ErrBadVerdict` via `errors.Is`, no regeneration; covers 277-278). Batch point 2 named the render half; the decode half of the same never-fire pair is pinned too, so the finding's own statement ("Statements 229-230, 233-234 … measure zero coverage") closes completely — `go tool cover`: `closePage` and `judgePage` now **100.0%** each. Helper change: `scriptedJudge` gained an `errOn` field (the n-th call errors instead of answering; zero keeps the old error-every-call behaviour, so the first-occurrence transport test is untouched). | **DONE** — all five red on re-applied mutants (below), green on the pristine tree |
| 2 | **M2** — "reference sheets persist immediately, as they render" is unpinned: a deferred end-of-phase persist survives the whole suite (round-1 P-A) | **`TestPersist_ReferenceSheetSurvivesSiblingStoreFailure`** (`persist_test.go`): two-member story, harness book with a cast row for member 1 (Mira) only, `Config.Limit: 1` serialises the phase. Member 1 renders and persists; member 2 (Bramble) renders and its `storeReference` fails loudly with `store.ErrInvalid` (the missing cast row fires on place); the returned Book is zero and Mira's sheet — read back via `CastMedia` and byte-checked against the first sheet render — is the only placed row. **Immediacy probe:** because the end state cannot distinguish immediate from deferred persist (a deferred post-`Wait` loop persists member 1 before member 2's `storeReference` fails — verified empirically, below), the fake imager additionally records, from *inside member 2's render*, whether member 1's sheet is already in the store. Under immediacy it must be (member 1's goroutine — render AND persist — fully completed before member 2's began); under a deferred persist nothing has been written at that moment. | **DONE** — red on P-A (below), green on the pristine tree. Pin deviates from the review's *suggested* assertion in one documented respect: the review's red-claim ("the phase aborts before anything is written") attaches to the end-state presence assertion, which a faithful P-A does NOT redden — the deferred loop persists member 1 before member 2's failure, so member 1 is present in both shapes. The red condition genuinely lives at the moment member 2 renders, so the pin observes the store there. Scenario, error sentinel, zero Book and end-state assertions are exactly the review's |
| 3 | **L1** — "Done counts pages that are final, not raw renders" is unpinned: reporting before the closing loop survives the whole suite (round-1 P-B) | **`TestIllustrate_DoneCountsFinalPagesNotRawRenders`** (`verify_test.go`): drift run (judge `false, true`) on `oneCastStory` (1 sheet + 1 page, Total 2), Progress wired to a mutex-guarded `progressLog` and the judge recording `len(progressEvents)` at every verdict. The judge's first call — on the not-yet-final initial render — must see only the sheet's event (Done 1 of 2): the drifted page's Progress event must not exist until the closing loop approves it. Both verdicts asserted at exactly 1 event; the final sequence still ends with the approved page reporting Done == Total (2 of 2), Stage Illustration, N 1. | **DONE** — red on P-B (below), green on the pristine tree |

## Mutation re-runs — the round's mutants re-applied, one pin at a time

Method per re-run (the round-1 reviewer's own): pristine source copied to `/tmp` → mutant applied → the single pin test run → restored from the copy → md5-compared against the round-1 manifest. **Every restore matched; no source file differs from the round-1 manifest, and the two test files are the only edits.**

| Pin | Mutant re-applied | Result on the pin | Restore md5 |
|---|---|---|---|
| `TestIllustrate_JudgeErrorMidLoopSurfacesUnretried` | **M4** (round-1 verbatim): `judgePage`'s error return in `closePage` → `continue` — a judge error regenerates instead of surfacing | **RED — hangs.** The error repeats (the judge's `seen` never advances past the erroring call) and no cap fires — the regeneration cap counts false verdicts, not errors. `-timeout 8s`: `FAIL thutapi/internal/illustrate 8.010s` (panic: test timed out), matching round-1 M4's own 8.009 s evidence. The return path is what bounds the loop | `verify.go 8dfa7bea…` ✓, pin green after restore |
| `TestIllustrate_RegenerationRenderFailureSurfacesUnretried` | Regen `EditImage` error return (229-230) → `continue`: a failed regeneration render regenerates anyway | **RED** — `err = illustrate: page 1: … still drifted after the regeneration cap …, want errors.Is(.., gmi.ErrTransient)`: the swallowed render error burned the retry budget to `ErrConsistency` instead of surfacing | `verify.go 8dfa7bea…` ✓ |
| `TestIllustrate_RegenerationDecodeFailureSurfacesUnretried` | Regen decode error return (233-234) → `continue`: an undecodable regeneration regenerates anyway | **RED** — `err = … still drifted after the regeneration cap …, want errors.Is(.., ErrUnsupportedImage)` | `verify.go 8dfa7bea…` ✓ |
| `TestIllustrate_NoChoicesReplyIsABadVerdict` | `len(resp.Choices) == 0` guard deleted (273-274) — the round-1 text's exact deletion | **RED — panic**: `panic: runtime error: index out of range [0] with length 0` — the guard is what stops an empty `Choices` from reaching `Choices[0]`; the reply is no longer a loud `ErrBadVerdict` | `verify.go 8dfa7bea…` ✓ |
| `TestIllustrate_TextlessAssistantReplyIsABadVerdict` | `Message.Text()` error wrap (277-278) → `return false, nil`: a textless reply reads as a drift verdict | **RED** — `err = … still drifted after the regeneration cap …, want errors.Is(.., ErrBadVerdict)`: the unverdictable reply silently burned three renders instead of failing the run loudly | `verify.go 8dfa7bea…` ✓ |
| `TestPersist_ReferenceSheetSurvivesSiblingStoreFailure` | **P-A** (round-1 verbatim): sheet persist hooks moved out of the fan-out goroutine into a post-`Wait` loop over the completed refs (`renderReferences`) | **RED** — `member 2's render saw member 1's sheet placed 0 times, want 1 — the sheet must persist inside member 1's goroutine, before the phase can fail`. (The end-state assertions — `store.ErrInvalid`, zero Book, Mira present — stay green under P-A, exactly the finding: a deferred persist is indistinguishable from immediacy by the end state alone.) | `illustrate.go 16e72216…` ✓, pin green after restore |
| `TestIllustrate_DoneCountsFinalPagesNotRawRenders` | **P-B** (round-1 verbatim): the page `Progress` report moved above the `closePage` hook (`renderPages`) | **RED** — `verdict 1 saw 2 Progress events, want 1` (and verdict 2): the not-yet-final page's event (Done 2 of 2) exists before the closing loop approved it | `illustrate.go 16e72216…` ✓, pin green after restore |

Also verified on the pristine tree: the review's own P-A green-claim still holds for the *existing* suite with the new pins present, minus the one new red pin above — i.e. the deferral mutant reddens exactly the new M2 pin and nothing else.

## Gates (run on the restored tree, new pins in place)

| Gate | Result |
| --- | --- |
| `go vet ./...` | clean |
| `go build ./...` | clean |
| `go test ./internal/illustrate/ -race -count=5` | `ok thutapi/internal/illustrate 3.469s` — no race, ordering stable |
| `go test ./... -race -count=1` | ok, all packages (`cmd`, `gmi/media`, `gmi/text`, `illustrate`, `interview`, `job`, `mediastore`, `store`, `story`, `stream`) |
| `gofmt -l .` | empty |
| coverage `internal/illustrate` | **95.5%** of statements (floor 75%); `verify.go` `closePage` and `judgePage` each **100.0%** — round-1 M1's zero-coverage statements (229-230, 233-234, 273-274, 277-278) are now exercised |

## Residue

* All three findings are DONE with pins that go red on reversion (evidence above) and green on the pristine tree. No production source file changed; no behaviour changed; the pins are the entire remediation, per the round's PIN-ONLY ruling.
* The review's M2 *suggested* assertion (end-state presence of member 1's sheet) was empirically checked against a faithful P-A and does not redden it — the deferred loop persists member 1 before member 2's `storeReference` fails. Recorded under disposition 2 so round 2 can re-verify the pin rather than the suggestion.
* Not remediated (not findings, per round-1's "Candidates examined and dismissed"): `place()`'s DB-failure branches, `storeReference`/`storePage`'s `blobs.Persist` error branches, and the near-echo perceptual gap — reaching the store-fault branches needs a fault seam no package in this patch exposes.
* **Zero residue is claimed only after round 2's verdict**: this record's dispositions are the round-2 reviewer's input, not its conclusion. Round 2 must re-run the mutation table above, re-check the round-1 findings, and issue its own APPROVE with an explicit zero-residue claim against this round.
