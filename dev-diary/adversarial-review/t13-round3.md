# T13 round 3 — adversarial review

| | |
|---|---|
| **Target** | T13 after remediation round 2: consented capture/upload, ffmpeg transcode, public source URL, clone request/response boundary, retention, route wiring, and adult-corner UI. |
| **Evidence read** | `AGENTS.md`; `PLAN.md` §T13; the touching decisions in `project.md`; `t13-round1.md`, `t13-remediation-round1.md`, `t13-round2.md`, and `t13-remediation-round2.md`; the current diff, `internal/audio/clone.go` and tests, GMI media client/tests, `cmd/thutapi`, deployment files, browser contract, mediastore, and generation/narration call paths. The user-authorized T12 compatibility hunks in `cmd/thutapi/main.go` and `main_test.go` were audited narrowly: supplying `Music: mediaCli` and changing the test renderer to the current duration-returning signature are correct. |
| **Verification** | One full `go test -count=1 -race ./...` pass, `go vet ./...`, `gofmt -l .`, `git diff --check`, `node --check static/app.js static/browser-test.js`, `bash -n deploy/docker-run.sh`, and `CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' ./cmd/thutapi` passed. The focused `go test -count=1 -race ./internal/audio ./internal/gmi/media ./cmd/thutapi` then failed: `TestShutdownLogRecordsSignalName` races on its shared log buffer. No deployed HTTPS consented sample or live GMI clone call was made. |

**Verdict: REMEDIATE — 1 C / 4 H / 0 M / 0 L.**

Round-one duration/output, recorder-MIME, static-ffmpeg, consent, and public-origin residues are closed. Round-two deployment configuration, bearer admission, private-origin rejection, and bounded duration-probe residues are closed. The five findings below remain: the cloned path accepts an invented provider result shape, never reaches narration even if that result were real, does not provide the promised durable deletion boundary, and the repository race gate is red.

| # | Severity | Where | What | Pin (failing probe or test) | Mutation (what breaks if reverted) |
| C1 | C | `cmd/thutapi/main.go:421-423`; `cmd/thutapi/main_test.go:732-749` | The shutdown test returns from `run` and reads `logBuf.String()` while the listener goroutine can still execute `log.Info("thutapi listening", ...)` into the same unsynchronised `bytes.Buffer`. The focused race suite reports concurrent `bytes.Buffer` read/write at exactly these lines. AGENTS.md classifies every race as C, so the track cannot pass its required race gate. This is outside T13's upload behaviour, but it is in a current T13-touched command/test seam and must be remediated by the owning workstream before approval. | `go test -count=1 -race -run '^TestShutdownLogRecordsSignalName$' ./cmd/thutapi` must pass repeatedly. It currently reports a data race between `run.func2`'s listener log and the test's buffer read. | Restore unsynchronised access or let `run` return before the listener goroutine is joined; the race detector reports the same concurrent buffer access. |
|---|---|---|---|---|---|
| H1 | H | `cmd/thutapi/main.go:85-91`; `internal/audio/clone.go:417-436`; `internal/audio/clone_test.go:307-317` | The code presents an unobserved response field as a verified clone identity. `gmiVoiceCloner` feeds raw queue bytes to `DecodeVoiceCloneResponse`; that decoder accepts root or `outcome.voice_id`, and the unit test blesses a fabricated `{"outcome":{"voice_id":"provider-voice"}}` reply. The source comments and T13 plan explicitly say the clone result and clone-to-HD handoff have not been observed. A mocked shape is not confirmation, so a queue response can yield 201 and a “ready” clone identity although no provider contract supports it. This violates the required no-invented-result, fail-closed boundary. | Add the T13b `//go:build live` probe with a consented operator-owned sample at the deployed HTTPS URL. It must save the terminal raw response and establish the response field plus an HD narration call using that id; until then every raw response must remain unverified and return 502. The current synthetic `outcome.voice_id` test produces 201 without either observation. | Reintroduce either assumed `voice_id` decode path before the live record; a mocked raw response again becomes a usable clone identity. |
| H2 | H | `static/app.js:549-553`; `internal/bookgen/bookgen.go:431-444`; `internal/audio/clone.go:283-290`; repository-wide `thutapi:voice-id` search | Even a genuinely verified id cannot affect the book. Continue writes it only to `sessionStorage`, then `POST /interviews/{id}/generate` has no body and no server-side reader; that key has no other consumer. `bookgen` invokes the existing narration flow with its configured library voice. Thus the successful clone UI path still generates exactly the default library-narrated book, leaving round-two H2's required clone-to-HD handoff unresolved. | Browser/server integration: make the upload return a verified clone id, choose Continue, and require the generation/narration seam to receive that exact id on its HD synthesis request. The current request is bodyless and no production code reads the session-storage key. | Remove the durable, server-side selected-voice handoff; a successful clone again disappears before narration. |
| H3 | H | `internal/audio/clone.go:313-367`; `static/app.js:264`; `internal/audio/clone_test.go:392-425` | “Expires after 15 minutes” means only “delete on a later `GET /media/{id}`.” There is no sweep, alarm, startup purge, or timer. An unguessable source URL normally receives only GMI's immediate fetch, so after that it can remain as a media row and blob indefinitely if nobody requests it again. The response is correctly `no-store` while served, but the adult disclosure promises that the app deletes it after 15 minutes and the stored child recording is not actually deleted then. | Persist a sample, advance an injected clock beyond its lifetime without requesting `/media/{id}`, and reopen the store/handler; assert its media row and blob are gone. The present test only proves deletion after it deliberately sends a later media GET. | Remove an automatic expiration sweep or startup cleanup; expired, never-refetched samples accumulate again. |
| H4 | H | `internal/audio/clone.go:543-553,350-415`; `internal/mediastore/mediastore.go:137-170`; `internal/audio/clone_test.go:392-425` | The expiry index is outside mediastore's durable media transaction and is written only after `Persist` has durably made the blob and row reachable. A process crash between those operations (or after a failed sidecar write followed by a restart) leaves an ordinary media object with no expiry-map entry. `handleMedia` then delegates it to mediastore's one-year immutable public-cache route. This is the media-deletion race/security gap: the sensitive recording can outlive both the 15-minute promise and the no-store override. | Fault-inject/kill the process immediately after `Persist` returns and before the expiry record is durably committed, restart with the same data dir, then require `GET /media/{id}` to be unavailable and the row/blob to be cleaned. The current handler reconstructs an empty expiry map and serves the orphan through ordinary mediastore policy. | Revert to a separate post-persist expiry sidecar without an atomic/fail-closed recovery rule; the crash-window media id becomes permanently untracked again. |
