# T13 round 2 — adversarial review

| | |
|---|---|
| **Target** | T13 after remediation round 1: voice-sample capture/upload/transcode, public `source_audio` handoff, consent UI, typed clone boundary, and runtime wiring. |
| **Evidence read** | `AGENTS.md`; `PLAN.md` §T13 and the T11 retention dependency; `project.md` §§Audio, Architecture consequences, and adult-facing flow; `t13-round1.md`; `t13-remediation-round1.md`; the current diff; `internal/audio/clone.go` and tests; `cmd/thutapi`; `static/app.js` and browser contract; mediastore; deployment runbook. |
| **Verification** | `go test -count=1 -race -cover ./internal/audio` passed (84.4%). `node --check static/app.js static/browser-test.js`, `gofmt -l .`, `bash -n deploy/docker-run.sh`, `git diff --check`, and the release `CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' ./cmd/thutapi` passed. The required full `go vet ./...` and `go test -count=1 -race ./...` do **not** pass in this shared tree: `cmd/thutapi/main_test.go:84` has an unrelated `failRenderer.Render` signature mismatch, `internal/bookgen/scratch_music_test.go:4` has an unused `net/http` import, and `internal/interview.TestRestartedHandlerSeesEndedInterview` timed out. The browser file was syntax-checked; no headless or physical-device runner is available here. No consented deployed-HTTPS clone probe or T13b live test exists. |

**Verdict: REMEDIATE — 1 C / 4 H / 1 M / 0 L.**

Round-one H2 (no silent `-fs` truncation/duration validation) and M1/M2 (recorder MIME negotiation and static ffmpeg path) have zero residue: the duration probe rejects over-15-second inputs before transcode, output is stat-checked after a no-`-fs` invocation, the browser chooses a supported accepted MIME, and `/usr/local/bin/ffmpeg` is pinned. The remaining round-one H1 and H3 defects are still observable, and the deployment and upload boundary add new defects below.

| # | Severity | Where | What | Pin (failing probe or test) | Mutation (what breaks if reverted) |
|---|---|---|---|---|---|
| C1 | C | `cmd/thutapi/main.go:107,288-291`; `deploy/docker-run.sh:146-156`; `deploy/README.md:133-139`; `.env.example` | T13 makes `PUBLIC_ORIGIN` mandatory: an empty value makes `NewVoiceSampleHandler` return `ErrNoVoiceSampleOrigin`, so `run` exits before listening. The documented production launcher passes only `PORT`, `DATA_DIR`, `GMI_API_KEY`, and the unused `UPLOAD_TOKEN`; neither it nor the deployment documentation nor `.env.example` supplies `PUBLIC_ORIGIN`. The normal deployment command therefore starts no application at all. | Launch the production image through `deploy/docker-run.sh` with its documented environment and assert `/healthz` answers. It currently exits with `build voice sample handler: audio: new voice sample handler: audio: no public voice sample origin configured`. Add an integration/config test that invokes `run` with deployment-equivalent environment and requires startup. | Remove `PUBLIC_ORIGIN` from the launcher or its documented required configuration while retaining T13's mandatory constructor validation; startup fails again before the health route is registered. |
| H1 | H | `internal/audio/clone.go:172-213`; `cmd/thutapi/main.go:288`; `deploy/docker-run.sh:102-114,155-156`; `deploy/README.md:138`; `README.md:53` | The public `POST /voice-sample` endpoint has no authentication or per-client admission control. It accepts any caller that supplies the literal header `X-Voice-Sample-Consent: yes`, runs ffmpeg, persists the result, and exposes it at a public media URL. Deployment generates, mounts, documents, and exports `UPLOAD_TOKEN` as this route's bearer, but no Go path reads or validates it; the consent header is not authentication or evidence of adult consent. This lets any internet client consume transcode/disk capacity and create publicly served audio. | Start the handler with a configured token, send a valid bounded multipart `POST /voice-sample` with only `X-Voice-Sample-Consent: yes`, and require 401/403 with no runner or store call. It currently returns 201. Add cases for missing, malformed, and wrong bearer values, and a valid bearer case. | Remove the bearer check (or treat the consent header as sufficient); the unauthenticated upload probe again obtains 201 and persists a public sample. |
| H2 | H | `static/app.js:175-190,545-549`; `internal/audio/clone.go:65-102`; repository live tests | Round-one H1 has not been remediated end-to-end. Capture now yields an absolute URL, but Continue only writes that URL to `sessionStorage` and invokes the unchanged generation request; no production path calls `VoiceCloner`, serializes the typed request into GMI's `{model,payload}` envelope, receives a real clone response, or carries a verified clone/HD `voice_id` into narration. `VoiceCloner` is an unused interface and the JSON test only marshals a manually constructed struct. There is also no T13b live probe using a consented deployed HTTPS sample to establish the unknown response/voice-id handoff. The UI's assertion that the library narrator remains selected is honest, but it demonstrates that the required clone feature is absent rather than complete. | Browser/server integration: upload a consented sample, choose Continue, and require the generation/narration seam to receive a provider-verified clone voice identifier, with a `//go:build live` T13b probe recording the deployed HTTPS request/response shape. Current code has no call site for `CloneVoice`; the only observable effect is `sessionStorage.setItem`. | Remove the source-audio handoff or leave the clone interface unused; the integration/live pin still cannot observe a clone request or a verified voice-id reaching narration. |
| H3 | H | `internal/audio/clone.go:192-197`; `internal/mediastore/mediastore.go:137-170,265-313`; `static/app.js:260`; T11 sweep status in `PLAN.md:121-125` | Round-one H3 also has behavior residue. A successful sample is an unplaced ordinary media row; no retention sweep exists, so after a process restart its unguessable URL remains served indefinitely. More seriously, the shared media route sets `Cache-Control: public, max-age=31536000, immutable`; even after a future origin-side T11 deletion, any browser/CDN that cached a child's source sample may serve it for up to a year. The adult copy says a retention sweep removes the sample, but no currently shipped lifecycle can make this public voice URL short-lived. | Persist a T13 sample, reopen the DB/store, then `GET /media/{id}` after the configured voice-sample expiry and require 404 plus removal of the blob. Also cache a response, expire/delete its origin row, and require its response headers prohibit continued shared-cache serving. Current restart GET is 200 with one-year public immutable caching. | Restore unplaced ordinary persistence or the immutable public cache policy for samples; the expiry/cache probe again exposes the child recording after its promised lifetime. |
| H4 | H | `internal/audio/clone.go:353-366`; `internal/audio/clone_test.go:252-264,359-375` | The claimed public-origin/source validation only checks URI syntax. It accepts `https://localhost`, `https://127.0.0.1`, `https://[::1]`, RFC1918/link-local IP literals, and arbitrary internal DNS hosts, then returns them as a supposedly public `source_audio` URL. GMI cannot fetch loopback/private endpoints, so a configuration typo yields a successful upload and unusable clone input; accepting internal hosts also defeats the stated public-origin trust boundary. The tests exercise malformed URLs but no non-public address. | Table-test `NewVoiceSampleHandler` and `NewVoiceCloneRequest` with loopback, unspecified, link-local, and RFC1918 HTTPS URLs and require their respective invalid-origin/source sentinels; require the configured known deployment host (or a resolver-backed publicly routable hostname) to pass. Current implementation accepts each syntactically valid HTTPS value. | Revert to scheme-and-host-only checks; the private-host cases again pass validation and return a provider-unfetchable source URL. |
| M1 | M | `internal/audio/clone.go:284-305,399-405`; `internal/audio/clone_test.go:224-250,377-384` | The new duration decision is only a parsed ffmpeg banner from a fake runner. The production `Duration` command decodes the entire untrusted input with `ffmpeg -f null -` before the handler applies the 15-second result, but has no process deadline separate from the request context and no real-fixture test. A crafted/slow-to-decode file under the byte cap can consume the request worker before it is rejected; the fake's `duration` field cannot prove that the real command detects duration or exits within a bound. | Use the shipping static ffmpeg with an actual over-15-second low-bitrate WebM/MP4 fixture under 5 MiB and assert 413, no persistence, a bounded context deadline, and no `-fs` argument. The present tests only inspect fake arguments/output and parse a literal banner. | Remove the bounded probe/deadline or replace it with the fake-only duration value; the real-file timeout/rejection test loses the operational protection it is meant to prove. |

No other new finding is recorded for direct shell injection, MIME negotiation, recorder gesture/permission fallback, or template XSS: the command uses a fixed `exec.CommandContext` argument vector; the browser negotiates accepted recorder types and keeps a file fallback; and Preact text interpolation escapes the UI's server/error strings.
