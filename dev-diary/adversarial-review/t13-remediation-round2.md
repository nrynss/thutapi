# T13 remediation round 2

| Finding | Fix | Verification |
|---|---|---|
| C1 | `PUBLIC_ORIGIN` is now a required runtime setting. `run` constructs the handler with it and fails before listening when it is empty, invalid, local, private, reserved, or an IP literal. The deployment launcher requires and passes it name-only into the container; `.env.example`, the public README, and the deploy runbook document the non-secret setting. | `TestRunRejectsMissingVoiceSampleConfiguration` pins fail-closed startup. `TestVoiceSampleHandler_MethodAndConfigErrors` and source/origin table cases pin invalid forms. `bash -n deploy/docker-run.sh` passed. |
| H1 | The upload handler requires both affirmative consent and `Authorization: Bearer <UPLOAD_TOKEN>` before reading the request body, starting ffmpeg, or persisting media. It compares a correctly formed bearer token in constant time and returns `401` plus a Bearer challenge for missing, malformed, or wrong credentials. The adult-only UI collects the one-time access code without embedding it in static assets. | `TestVoiceSampleHandler_BearerAuthorizationRequired` covers missing, malformed, wrong, and valid bearers and proves rejected calls do not reach runner/store. Browser contract now pins the Authorization header. |
| H2 | A successful consented upload persists the short-lived source then invokes `gmi/media.Client.CloneVoice` through the narrow `gmiVoiceCloner` adapter. The client emits the confirmed `{model,payload}` request-queue envelope, including the documented `need_volumn_normalization` key. `DecodeVoiceCloneResponse` accepts only typed `voice_id` fields; unknown/missing shapes return `ErrVoiceCloneUnverified` and the request receives a visible `502`, never a fabricated ID. The library narrator remains the default when no verified clone is returned. | `TestCloneVoice_TypoPinnedInRawJSON` pins the full mock request envelope; `TestVoiceSampleHandler_CloneRequestAndUnverifiedResponse` proves the handler invokes the cloner and fails loud on no voice ID; `TestDecodeVoiceCloneResponse_FailsClosed` pins typed response handling. No live clone or real voice sample was used. |
| H3 | T13 records each sample id and expiry in a mode-0600 durable sidecar under `DATA_DIR`, so restart does not extend its life. Tracked samples expire after 15 minutes, delete their mediastore row/blob on the next fetch, and flow through a response writer that forces `Cache-Control: no-store`; ordinary generated media retains mediastore's immutable policy. | `TestVoiceSampleHandler_ExpiresAndNeverUsesImmutableCache` reloads the expiry index, proves the response is no-store, then advances the clock and proves the sample is deleted and 404. |
| H4 | Origin/source validation now requires HTTPS plus a public hostname and rejects userinfo, path/query/fragment misuse, localhost, `.local`, `.internal`, reserved DNS suffixes, and every IP literal. | `TestNewVoiceCloneRequest_RequiresAbsoluteHTTPSSource` and handler config invalid-origin cases cover loopback, IPv4, IPv6, RFC1918, local, and internal inputs. |
| M1 | The duration preflight runs under its own five-second context deadline before transcode; ffmpeg remains a fixed, shell-free argument vector and still has no `-fs` truncation flag. | `ffmpegFake.Duration` rejects a missing deadline; `TestVoiceSampleHandler_LongPreparedFileRejectedBeforeTranscode` and `TestVoiceSampleHandler_FilesizeFlagMutationCannotSilentlyClip` remain green. |

Validation passed: `go test -count=1 -race ./...`, `go vet ./...`, `CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' ./cmd/thutapi`, `gofmt -l .`, `node --check static/app.js`, `bash -n deploy/docker-run.sh`, and `git diff --check`. `node static/browser-test.js` cannot run standalone because it requires its browser `document` environment; the browser contract was syntax-checked and updated, but no headless browser runner is installed here.

The requested T12 compatibility hunks in `cmd/thutapi` are also present: the test renderer implements the current duration-returning signature, and production passes `Music: mediaCli` to `bookgen.New`.
